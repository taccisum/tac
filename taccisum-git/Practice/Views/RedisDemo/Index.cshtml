@using Practice.Models

@{
    ViewBag.Title = "Index";
    ViewBag.Crumbs = new Crumbs(new List<CrumbPath>()
    {
        new CrumbPath("系统管理"),
        new CrumbPath("菜单缓存"),
    });
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <span class="bold">最近添加的菜单列表：</span><br/>
    <textarea id="txt-menus" style="width: 900px; height: 200px;"></textarea>
</div>

@section head{
    <script>
        require(["systools"], function(tool) {
            $.get("/Menu/GetRecentlyAdd", function (result) {
                if (result.Success) {
                    var str = "";
                    $.each(result.Data, function (index, item) {
                        str += item + "\r\n";
                    });
                    $("#txt-menus").val(str);
                } else {
                    tool.msgbox("<div class='msgbox-text'><span class='text-left'>" + result.Message + "</span>" +
                        "<br/><a href='javascript:alert(\"" + result.Exception + "\")' color='red pull-right'>查看异常</a>", "n");
                }
            });

            $("#btn-easydialog").on("click", function () {
                tool.easydialog("#tac", function(args, $content) {
                    
                });
            });
        })
    </script>
}

<button id="btn-easydialog" type="button" class="btn btn-success btn-sm">popup</button>

<div class="hidden">
    <div id="tac" data-title="hhh" data-width="600px" data-height="400px" class="container" style="width: 600px;">
        <div class="row">
            <div class="col-md-6">
                <div class="input-group">
                    <span class="input-group-addon bold">hijk</span>
                    <input class="form-control" name="Field1" />
                </div>
            </div>
            <div class="col-md-6">
                <div class="input-group">
                    <span class="input-group-addon bold">lmn</span>
                    <input class="form-control" name="Field2" />
                </div>
            </div>
        </div>
        <div class="emptyRow"></div>
        <div>
            <input type="text" />
        </div>
    </div>
</div>
